#variable has been specified logically 
x = 1       

# x += ??? Logically no index variable (iterator) is established, to determine the number of iteration in either directions.

# x is conditioned to numbers less than 10 which is never true
while (x < 10):

    # the system will logically produce an infinite loops with a 111111...
    # the program will run but not reaching the programmer's expectations.
    print(x)
